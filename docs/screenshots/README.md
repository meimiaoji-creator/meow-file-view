# docs/screenshots · 截图清单

README 引用的截图放本目录。在本地 dsh web profile 装好本插件后截**真实图**，按下面的文件名覆盖即可。

| 文件 | 截什么 | README 引用位置 |
|---|---|---|
| `file-panel.png` | 点输入框「文件」按钮后的右侧面板：左目录树 + 右侧一个 md 预览（带 TOC 最佳） | 「功能」开头 |
| `artifacts-diff.png` | 某回合尾部的产物 chips 行，其中至少一个 chip 长出「Diff」按钮（或点开后的差异视图） | 「回合产物行 + Git 差异」 |
| `settings-git.png` | 设置弹窗里 meow-file-view 的「Git 差异」节（gitExePath + repoRoots 表） | 「设置页 Git 差异节」 |

要求：图片清晰、能看到工作区真实内容即可；不要包含 token / 隐私路径。
